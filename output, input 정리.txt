문자열을 byte[] 배열로 바꿔주는 메서드 .getBytes()

.write 를 쓰면 말그대로 그 문자를 fos에다 쓴다는것이다

("/io_test/20240222/바이트기반_테스트.txt"); 

이렇게 만들어놧엇으니까 맨뒤에 테스트.txt 에 써지게 된다는것이다

이제 썻으면 밀어 넣어야 된다

flush() 메서드를 쓰면 된다


다쓰고 나면 fianlly 구문에서 fos의 통로를 닫아줘야 메모리 누수가 생기지 않는다

1.문자열을 byte객체에 넣고 
2. 그 객체를 fos에다가 전달하고 밀어넣는다

ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ

readAllBytes() 메서드 알아서 그 문자들을 byte형태로 가져온다

String str = new String(bytes); 을쓰면 알아서 byte에 저장된 문자열들이 string 문자열로 자동으로 바뀐다




